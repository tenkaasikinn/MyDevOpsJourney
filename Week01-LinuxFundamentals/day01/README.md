# 🧠 Day 1 — Command Line Interface (CLI) Fundamentals

📅 **Tanggal:** _11 November 2025_  
🎯 **Tujuan:** Kuasai navigasi sistem dan manipulasi file tanpa GUI.

---

## 🔧 Fokus Hari Ini
- Navigasi direktori dan file system (`pwd`, `cd`, `ls`, `cat`)
- Mencari dan memfilter data (`find`, `grep`, `awk`)
- Manipulasi teks (`sed`, `cut`, `sort`, `uniq`)
- Membuat dokumentasi awal: `cli_cheatsheet.md`

---

## 🧩 Insight
Beberapa hal penting yang saya pelajari hari ini:
- Fedora tidak pakai `/var/log/syslog`, tapi `journalctl` buat lihat log.
- Kombinasi `grep` + `awk` bikin parsing data super cepat.
- Absolute vs relative path itu krusial pas scripting nanti.

---

## 📂 File yang Dibuat
- `awk_level1.sh` Berisi awk bagian -F dan limiter
- `awk_level2.sh` Berisi awk bagian filter kata, filter kolom, header & footer, if